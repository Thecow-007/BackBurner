#!/usr/bin/env node
// Phase 0 stub. The real seed CLI (docs/architecture.md §12,
// docs/test-plan.md §5.9) composes the engine's seed module with the api
// package's user provisioning; it lands in Phase 4.
console.log("[seed] not implemented until Phase 4");
process.exit(0);
